####################
# Code generated by EntPy - do not modify manually!
####################

from __future__ import annotations
import logging
from entpy import (
    db,
    Ent,
    generate_uuid,
    ExecutionError,
    Action,
    Decision,
)
from uuid import UUID
from datetime import datetime, UTC
from evc import ExampleViewerContext
from .ent_model import EntModel
from ent_test_object3_schema import EntTestObject3Schema
from entpy import EdgeDelegate, PrivacyRule
from entpy import Field
from entpy import PrivacyError
from entpy.framework.ent import EntObjectBase
from entpy.framework.query import EntObjectQuery
from entpy.model import APIEntity
from pydantic import Field as APIField
from rules import AllowIfOmniscientViewerContext
from rules import AllowIfTestViewerContext
from rules import DenyIfSoftDeleted
from sentinels import Sentinel  # type: ignore[import-untyped]
from sqlalchemy import ForeignKey
from sqlalchemy import UUID as DBUUID
from sqlalchemy.orm import Mapped, mapped_column
from sqlalchemy.orm import relationship
from typing import TYPE_CHECKING

if TYPE_CHECKING:
    from .ent_test_object4 import EntTestObject4Model
    from .ent_test_object4 import EntTestObject4APIModel
    from .ent_test_object4 import EntTestObject4

privacy_logger = logging.getLogger("entpy.privacy")


class EntTestObject3Model(EntModel):
    __tablename__ = "test_object3"

    other_id: Mapped[UUID | None] = mapped_column(
        DBUUID(),
        ForeignKey("test_object4.id", deferrable=True, initially="DEFERRED"),
        nullable=True,
    )
    other: Mapped["EntTestObject4Model"] = relationship(
        "EntTestObject4Model",
        primaryjoin="EntTestObject3Model.other_id == EntTestObject4Model.id",
    )


class EntTestObject3APIModel(APIEntity):
    other: "EntTestObject4APIModel | None" = APIField(None)


class EntTestObject3(EntObjectBase[ExampleViewerContext, EntTestObject3Model]):
    m = EntTestObject3Model

    if TYPE_CHECKING:
        other_id: UUID | None

    async def gen_other(self) -> "EntTestObject4" | None:
        from .ent_test_object4 import EntTestObject4

        if self.model.other_id:
            return await EntTestObject4.gen(self.vc, self.model.other_id)
        return None

    async def _gen_evaluate_privacy(
        self,
        vc: ExampleViewerContext,
        action: Action,
        default_to_deny: bool = True,
        log_on_deny: bool = True,
    ) -> Decision:
        # Build the complete list: prepended rules + entity's config
        prepended_rules: list[PrivacyRule] = []
        if action in [
            Action.READ,
            Action.CREATE,
            Action.UPDATE,
            Action.HARD_DELETE,
            Action.SOFT_DELETE,
        ]:
            prepended_rules.append(AllowIfTestViewerContext())
        if action in [Action.READ]:
            prepended_rules.append(AllowIfOmniscientViewerContext())
        if action in [Action.READ]:
            prepended_rules.append(DenyIfSoftDeleted())

        config = EntTestObject3Schema().get_privacy_config(action)
        all_rules = prepended_rules + config

        # Evaluate each rule/delegate in order
        for item in all_rules:
            if isinstance(item, PrivacyRule):
                decision = await item.gen_evaluate_cached(vc, self)
                if decision == Decision.DENY and log_on_deny:
                    privacy_logger.debug(
                        "Privacy rule %s of EntTestObject3 with ID %s was denied for %s",
                        type(item),
                        self.id,
                        str(vc),
                    )
            elif isinstance(item, EdgeDelegate):
                delegate = await self._gen_load_delegate(vc, item.edge_name)
                decision = await delegate._gen_evaluate_privacy(
                    vc, action, default_to_deny=False
                )
                if decision == Decision.DENY and log_on_deny:
                    privacy_logger.debug(
                        "Delegate privacy of EntTestObject3 with ID %s to edge %s was denied for %s",
                        self.id,
                        item.edge_name,
                        str(vc),
                    )
            else:
                raise ExecutionError(
                    "An invalid privacy configuration was found for EntTestObject3: invalid item type in list"
                )
            # If we get an ALLOW or DENY, we return instantly. Else, we keep going.
            if decision != Decision.PASS:
                return decision
        # Return based on default behavior
        if default_to_deny:
            if log_on_deny:
                privacy_logger.debug(
                    "Defaulting to denying access to EntTestObject3 with ID %s after exhausting all privacy rules for %s",
                    self.id,
                    str(vc),
                )
            return Decision.DENY
        return Decision.PASS

    async def _gen_load_delegate(self, vc: ExampleViewerContext, edge_name: str) -> Ent:
        raise ExecutionError(
            f"An invalid privacy configuration was found for EntTestObject3: could not find delegate for {edge_name}"
        )

    @classmethod
    def query(cls, vc: ExampleViewerContext) -> EntTestObject3Query:
        return EntTestObject3Query(vc=vc)


class EntTestObject3Query(
    EntObjectQuery[ExampleViewerContext, EntTestObject3, EntTestObject3Model]
):
    ent_type = EntTestObject3
    model_type = EntTestObject3Model


class EntTestObject3Mutator:
    @classmethod
    def create(
        cls,
        vc: ExampleViewerContext,
        other_id: UUID | None = None,
        id: UUID | None = None,
        created_at: datetime | None = None,
        updated_at: datetime | None = None,
    ) -> EntTestObject3MutatorCreationAction:
        return EntTestObject3MutatorCreationAction(
            vc=vc,
            id=id,
            created_at=created_at,
            updated_at=updated_at,
            other_id=other_id,
        )

    @classmethod
    def update(
        cls, vc: ExampleViewerContext, ent: EntTestObject3
    ) -> EntTestObject3MutatorUpdateAction:
        return EntTestObject3MutatorUpdateAction(vc=vc, ent=ent)

    @classmethod
    def hard_delete(
        cls, vc: ExampleViewerContext, ent: EntTestObject3
    ) -> EntTestObject3MutatorDeletionAction:
        return EntTestObject3MutatorDeletionAction(vc=vc, ent=ent, is_soft_delete=False)

    @classmethod
    def soft_delete(
        cls, vc: ExampleViewerContext, ent: EntTestObject3
    ) -> EntTestObject3MutatorDeletionAction:
        return EntTestObject3MutatorDeletionAction(vc=vc, ent=ent, is_soft_delete=True)


class EntTestObject3MutatorCreationAction:
    vc: ExampleViewerContext
    id: UUID
    other_id: UUID | None = None

    def __init__(
        self,
        vc: ExampleViewerContext,
        id: UUID | None,
        created_at: datetime | None,
        updated_at: datetime | None,
        other_id: UUID | None,
    ) -> None:
        self.vc = vc
        self.created_at = created_at if created_at else datetime.now(tz=UTC)
        self.updated_at = updated_at if updated_at else self.created_at
        self.id = id if id else generate_uuid(EntTestObject3, self.created_at)
        self.other_id = other_id

    async def gen_savex(self) -> EntTestObject3:
        model = EntTestObject3Model(
            id=self.id,
            updated_at=self.updated_at,
            created_at=self.created_at,
            other_id=self.other_id,
        )
        db.session.add(model)
        ent = EntTestObject3(vc=self.vc, model=model)
        decision = await ent._gen_evaluate_privacy(vc=self.vc, action=Action.CREATE)
        if decision != Decision.ALLOW:
            raise PrivacyError(
                f"Current viewer context is not authorized to CREATE EntTestObject3 with ID {ent.id}"
            )
        await db.session.flush()
        return await EntTestObject3._genx_from_model(self.vc, model)  # noqa: SLF001


class EntTestObject3MutatorUpdateAction:
    vc: ExampleViewerContext
    ent: EntTestObject3
    id: UUID
    other_id: UUID | None = None

    def __init__(self, vc: ExampleViewerContext, ent: EntTestObject3) -> None:
        self.vc = vc
        self.ent = ent
        self.other_id = ent.other_id

    async def gen_savex(self) -> EntTestObject3:
        model = self.ent.model
        model.other_id = self.other_id
        model.updated_at = datetime.now(tz=UTC)
        db.session.add(model)
        new_ent = EntTestObject3(vc=self.vc, model=model)
        decision = await new_ent._gen_evaluate_privacy(vc=self.vc, action=Action.UPDATE)
        if decision != Decision.ALLOW:
            raise PrivacyError(
                f"Current viewer context is not authorized to UPDATE EntTestObject3 with ID {new_ent.id}"
            )
        await db.session.flush()
        await db.session.refresh(model)
        return await EntTestObject3._genx_from_model(self.vc, model)  # noqa: SLF001


class EntTestObject3MutatorDeletionAction:
    vc: ExampleViewerContext
    ent: EntTestObject3

    def __init__(
        self, vc: ExampleViewerContext, ent: EntTestObject3, is_soft_delete: bool
    ) -> None:
        self.vc = vc
        self.ent = ent
        self.is_soft_delete = is_soft_delete

    async def gen_save(self) -> None:
        model = self.ent.model
        action = Action.SOFT_DELETE if self.is_soft_delete else Action.HARD_DELETE
        decision = await self.ent._gen_evaluate_privacy(vc=self.vc, action=action)
        if decision != Decision.ALLOW:
            raise PrivacyError(
                f"Current viewer context is not authorized to {action} EntTestObject3 with ID {self.ent.id}"
            )
        if self.is_soft_delete:
            model.soft_deleted_at = datetime.now(tz=UTC)
            model.updated_at = datetime.now(tz=UTC)
            db.session.add(model)
        else:
            await db.session.delete(model)
        await db.session.flush()


class EntTestObject3Example:
    @classmethod
    async def gen_create(
        cls,
        vc: ExampleViewerContext,
        created_at: datetime | None = None,
        other_id: UUID | None = None,
    ) -> EntTestObject3:
        # TODO make sure we only use this in test mode

        if isinstance(other_id, Sentinel) or other_id is None:
            from .ent_test_object4 import EntTestObject4Example

            other_id_ent = await EntTestObject4Example.gen_create(vc)
            other_id = other_id_ent.id

        return await EntTestObject3Mutator.create(
            vc=vc, created_at=created_at, other_id=other_id
        ).gen_savex()


def _get_field(field_name: str) -> Field:
    schema = EntTestObject3Schema()
    fields = schema.get_all_fields()
    field = next(
        filter(
            lambda field: field.name == field_name,
            fields,
        )
    )
    if not field:
        raise ValueError(f"Unknown field: {field_name}")
    return field
