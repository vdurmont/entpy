####################
# Code generated by EntPy - do not modify manually!
####################

from __future__ import annotations
import logging
from entpy import (
    Ent,
)
from uuid import UUID
from datetime import datetime
from evc import ExampleViewerContext
from .ent_model import EntModel
from ent_user_schema import EntUserSchema
from entpy.framework.ent import EntObjectBase
from entpy.framework.mutators import (
    EntMutatorCreationAction,
    EntMutatorUpdateAction,
    EntMutatorDeletionAction,
)
from entpy.framework.query import EntObjectQuery
from entpy.model import APIEntity
from functools import cache
from privacy import PrivacyMixin
from pydantic import Field as APIField
from sentinels import NOTHING, Sentinel  # type: ignore[import-untyped]
from sqlalchemy import String
from sqlalchemy.orm import Mapped, mapped_column
from typing import TYPE_CHECKING

if TYPE_CHECKING:
    from entpy import Ent
    from entpy import Field

privacy_logger = logging.getLogger("entpy.privacy")


class EntUserModel(EntModel):
    __tablename__ = "user"

    email: Mapped[str] = mapped_column(String(255, collation="nocase"), nullable=False)
    name: Mapped[str] = mapped_column(String(100), nullable=False)
    contact_email: Mapped[str | None] = mapped_column(
        String(255, collation="nocase"),
        nullable=True,
        server_default="noreply@example.com",
    )
    secondary_email: Mapped[str | None] = mapped_column(
        String(255, collation="nocase"), nullable=True
    )


class EntUserAPIModel(APIEntity):
    email: str = APIField(..., examples=["john.doe@example.com"])
    name: str = APIField(..., examples=["John Doe"])
    contact_email: str | None = APIField("noreply@example.com")
    secondary_email: str | None = APIField(None, examples=["john.backup@example.com"])


class EntUser(PrivacyMixin, EntObjectBase[ExampleViewerContext, EntUserModel]):
    """
    A user entity with email validation
    """

    m = EntUserModel
    schema = EntUserSchema()

    if TYPE_CHECKING:
        email: str
        name: str
        contact_email: str | None
        secondary_email: str | None

    @classmethod
    @cache
    def _get_edge_type(cls, edge_name: str) -> tuple[type[Ent], bool]:
        return super()._get_edge_type(edge_name)

    @classmethod
    def query(
        cls,
        vc: ExampleViewerContext,
    ) -> EntUserQuery:
        return EntUserQuery(vc=vc)


class EntUserQuery(EntObjectQuery[ExampleViewerContext, EntUser, EntUserModel]):
    ent_type = EntUser
    model_type = EntUserModel


class EntUserMutator:
    @classmethod
    def create(
        cls,
        vc: ExampleViewerContext,
        email: str,
        name: str,
        contact_email: str | None = None,
        secondary_email: str | None = None,
        id: UUID | None = None,
        created_at: datetime | None = None,
        updated_at: datetime | None = None,
    ) -> EntUserMutatorCreationAction:
        return EntUserMutatorCreationAction(
            vc=vc,
            id=id,
            created_at=created_at,
            updated_at=updated_at,
            email=email,
            name=name,
            contact_email=contact_email,
            secondary_email=secondary_email,
        )

    @classmethod
    def update(
        cls, vc: ExampleViewerContext, ent: EntUser
    ) -> EntUserMutatorUpdateAction:
        return EntUserMutatorUpdateAction(vc=vc, ent=ent)

    @classmethod
    def hard_delete(
        cls, vc: ExampleViewerContext, ent: EntUser
    ) -> EntUserMutatorDeletionAction:
        return EntUserMutatorDeletionAction(vc=vc, ent=ent, is_soft_delete=False)

    @classmethod
    def soft_delete(
        cls, vc: ExampleViewerContext, ent: EntUser
    ) -> EntUserMutatorDeletionAction:
        return EntUserMutatorDeletionAction(vc=vc, ent=ent, is_soft_delete=True)


class EntUserMutatorCreationAction(
    EntMutatorCreationAction[ExampleViewerContext, EntUser, EntUserModel]
):
    ent_type = EntUser
    model_type = EntUserModel
    schema = EntUserSchema()
    vc: ExampleViewerContext

    if TYPE_CHECKING:
        id: UUID
        email: str
        name: str
        contact_email: str | None = None
        secondary_email: str | None = None


class EntUserMutatorUpdateAction(
    EntMutatorUpdateAction[ExampleViewerContext, EntUser, EntUserModel]
):
    ent_type = EntUser
    model_type = EntUserModel
    schema = EntUserSchema()
    vc: ExampleViewerContext
    ent: EntUser

    if TYPE_CHECKING:
        id: UUID
        email: str
        name: str
        contact_email: str | None = None
        secondary_email: str | None = None


class EntUserMutatorDeletionAction(
    EntMutatorDeletionAction[ExampleViewerContext, EntUser, EntUserModel]
):
    ent_type = EntUser


class EntUserExample:
    @classmethod
    async def gen_create(
        cls,
        vc: ExampleViewerContext,
        created_at: datetime | None = None,
        email: str | Sentinel = NOTHING,
        name: str | Sentinel = NOTHING,
        contact_email: str | None = None,
        secondary_email: str | Sentinel = NOTHING,
    ) -> EntUser:
        # TODO make sure we only use this in test mode

        email = "john.doe@example.com" if isinstance(email, Sentinel) else email

        name = "John Doe" if isinstance(name, Sentinel) else name

        contact_email = (
            "noreply@example.com"
            if isinstance(contact_email, Sentinel)
            else contact_email
        )

        secondary_email = (
            "john.backup@example.com"
            if isinstance(secondary_email, Sentinel)
            else secondary_email
        )

        return await EntUserMutator.create(
            vc=vc,
            created_at=created_at,
            email=email,
            name=name,
            contact_email=contact_email,
            secondary_email=secondary_email,
        ).gen_savex()


def _get_field(field_name: str) -> Field:
    schema = EntUserSchema()
    fields = schema.get_all_fields()
    field = next(
        filter(
            lambda field: field.name == field_name,
            fields,
        )
    )
    if not field:
        raise ValueError(f"Unknown field: {field_name}")
    return field
