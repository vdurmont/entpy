####################
# Code generated by EntPy - do not modify manually!
####################

from __future__ import annotations
import logging
from entpy import (
    Ent,
)
from uuid import UUID
from datetime import datetime
from evc import ExampleViewerContext
from .ent_model import EntModel
from ent_pydantic_test_schema import AddressShape
from ent_pydantic_test_schema import EntPydanticTestSchema
from ent_pydantic_test_schema import ProfileShape
from entpy.framework.ent import EntObjectBase
from entpy.framework.mutators import (
    EntMutatorCreationAction,
    EntMutatorUpdateAction,
    EntMutatorDeletionAction,
)
from entpy.framework.query import EntObjectQuery
from entpy.model import APIEntity
from functools import cache
from privacy import PrivacyMixin
from pydantic import Field as APIField
from sentinels import NOTHING, Sentinel  # type: ignore[import-untyped]
from sqlalchemy import JSON
from sqlalchemy import String
from sqlalchemy.dialects.postgresql import JSONB
from sqlalchemy.orm import Mapped, mapped_column
from typing import Any
from typing import TYPE_CHECKING

if TYPE_CHECKING:
    from entpy import Ent
    from entpy import Field

privacy_logger = logging.getLogger("entpy.privacy")


class EntPydanticTestModel(EntModel):
    __tablename__ = "pydantic_test"

    address: Mapped[dict[str, Any]] = mapped_column(
        JSON().with_variant(JSONB(), "postgresql"), nullable=False
    )
    name: Mapped[str] = mapped_column(String(100), nullable=False)
    legacy_data: Mapped[dict[str, Any] | None] = mapped_column(
        JSON().with_variant(JSONB(), "postgresql"), nullable=True
    )
    profile: Mapped[dict[str, Any] | None] = mapped_column(
        JSON().with_variant(JSONB(), "postgresql"), nullable=True
    )


class EntPydanticTestAPIModel(APIEntity):
    address: AddressShape = APIField(
        ...,
        examples=[{"street": "123 Main St", "city": "New York", "zip_code": "10001"}],
    )
    name: str = APIField(..., examples=["John Doe"])
    legacy_data: dict[str, Any] | None = APIField(None, examples=[{"foo": "bar"}])
    profile: ProfileShape | None = APIField(
        None,
        examples=[
            {"bio": "Software engineer", "age": 30, "hobbies": ["coding", "reading"]}
        ],
    )


class EntPydanticTest(
    PrivacyMixin, EntObjectBase[ExampleViewerContext, EntPydanticTestModel]
):
    m = EntPydanticTestModel
    schema = EntPydanticTestSchema()

    if TYPE_CHECKING:

        @property
        def name(self) -> str:
            pass

        @property
        def legacy_data(self) -> dict[str, Any] | None:
            pass

    @property
    def address(self) -> AddressShape:
        raw = self.model.address
        if raw is None:
            return None
        return AddressShape.model_validate(raw)

    @property
    def address_raw(self) -> dict[str, Any]:
        return self.model.address

    @property
    def profile(self) -> ProfileShape | None:
        raw = self.model.profile
        if raw is None:
            return None
        return ProfileShape.model_validate(raw)

    @property
    def profile_raw(self) -> dict[str, Any] | None:
        return self.model.profile

    @classmethod
    @cache
    def _get_edge_type(cls, edge_name: str) -> tuple[type[Ent], bool]:
        return super()._get_edge_type(edge_name)

    @classmethod
    def query(
        cls,
        vc: ExampleViewerContext,
    ) -> EntPydanticTestQuery:
        return EntPydanticTestQuery(vc=vc)


class EntPydanticTestQuery(
    EntObjectQuery[ExampleViewerContext, EntPydanticTest, EntPydanticTestModel]
):
    ent_type = EntPydanticTest
    model_type = EntPydanticTestModel


class EntPydanticTestMutator:
    @classmethod
    def create(
        cls,
        vc: ExampleViewerContext,
        address: AddressShape | dict[str, Any],
        name: str,
        legacy_data: dict[str, Any] | None = None,
        profile: ProfileShape | dict[str, Any] | None = None,
        id: UUID | None = None,
        created_at: datetime | None = None,
        updated_at: datetime | None = None,
    ) -> EntPydanticTestMutatorCreationAction:
        return EntPydanticTestMutatorCreationAction(
            vc=vc,
            id=id,
            created_at=created_at,
            updated_at=updated_at,
            address=address,
            name=name,
            legacy_data=legacy_data,
            profile=profile,
        )

    @classmethod
    def update(
        cls, vc: ExampleViewerContext, ent: EntPydanticTest
    ) -> EntPydanticTestMutatorUpdateAction:
        return EntPydanticTestMutatorUpdateAction(vc=vc, ent=ent)

    @classmethod
    def hard_delete(
        cls, vc: ExampleViewerContext, ent: EntPydanticTest
    ) -> EntPydanticTestMutatorDeletionAction:
        return EntPydanticTestMutatorDeletionAction(
            vc=vc, ent=ent, is_soft_delete=False
        )

    @classmethod
    def soft_delete(
        cls, vc: ExampleViewerContext, ent: EntPydanticTest
    ) -> EntPydanticTestMutatorDeletionAction:
        return EntPydanticTestMutatorDeletionAction(vc=vc, ent=ent, is_soft_delete=True)


class EntPydanticTestMutatorCreationAction(
    EntMutatorCreationAction[
        ExampleViewerContext, EntPydanticTest, EntPydanticTestModel
    ]
):
    ent_type = EntPydanticTest
    model_type = EntPydanticTestModel
    schema = EntPydanticTestSchema()
    vc: ExampleViewerContext

    if TYPE_CHECKING:
        id: UUID
        address: AddressShape | dict[str, Any]
        name: str
        legacy_data: dict[str, Any] | None = None
        profile: ProfileShape | dict[str, Any] | None = None

    def __init__(
        self,
        vc: ExampleViewerContext,
        id: UUID | None,
        created_at: datetime | None,
        updated_at: datetime | None,
        **kwargs: Any,
    ) -> None:
        # Process Pydantic fields before passing to model
        if "address" in kwargs and kwargs["address"] is not None:
            value = kwargs["address"]
            if isinstance(value, AddressShape):
                kwargs["address"] = value.model_dump(mode="json")
            elif isinstance(value, dict):
                kwargs["address"] = AddressShape.model_validate(value).model_dump(
                    mode="json"
                )
        if "profile" in kwargs and kwargs["profile"] is not None:
            value = kwargs["profile"]
            if isinstance(value, ProfileShape):
                kwargs["profile"] = value.model_dump(mode="json")
            elif isinstance(value, dict):
                kwargs["profile"] = ProfileShape.model_validate(value).model_dump(
                    mode="json"
                )
        super().__init__(
            vc=vc, id=id, created_at=created_at, updated_at=updated_at, **kwargs
        )

    if not TYPE_CHECKING:

        def __setattr__(self, name: str, value: Any) -> None:
            if hasattr(self, "model") and name in self.model.__table__.columns:
                # Handle Pydantic JsonFields
                if name == "address" and value is not None:
                    # Accept both Pydantic instances and dicts
                    if isinstance(value, AddressShape):
                        value = value.model_dump(mode="json")
                    elif isinstance(value, dict):
                        # Validate dict by parsing it
                        value = AddressShape.model_validate(value).model_dump(
                            mode="json"
                        )
                if name == "profile" and value is not None:
                    # Accept both Pydantic instances and dicts
                    if isinstance(value, ProfileShape):
                        value = value.model_dump(mode="json")
                    elif isinstance(value, dict):
                        # Validate dict by parsing it
                        value = ProfileShape.model_validate(value).model_dump(
                            mode="json"
                        )
                setattr(self.model, name, value)
            else:
                super().__setattr__(name, value)


class EntPydanticTestMutatorUpdateAction(
    EntMutatorUpdateAction[ExampleViewerContext, EntPydanticTest, EntPydanticTestModel]
):
    ent_type = EntPydanticTest
    model_type = EntPydanticTestModel
    schema = EntPydanticTestSchema()
    vc: ExampleViewerContext
    ent: EntPydanticTest

    if TYPE_CHECKING:
        id: UUID
        address: AddressShape | dict[str, Any]
        name: str
        legacy_data: dict[str, Any] | None = None
        profile: ProfileShape | dict[str, Any] | None = None

    if not TYPE_CHECKING:

        def __setattr__(self, name: str, value: Any) -> None:
            if hasattr(self, "model") and name in self.model.__table__.columns:
                # Handle Pydantic JsonFields
                if name == "address" and value is not None:
                    # Accept both Pydantic instances and dicts
                    if isinstance(value, AddressShape):
                        value = value.model_dump(mode="json")
                    elif isinstance(value, dict):
                        # Validate dict by parsing it
                        value = AddressShape.model_validate(value).model_dump(
                            mode="json"
                        )
                if name == "profile" and value is not None:
                    # Accept both Pydantic instances and dicts
                    if isinstance(value, ProfileShape):
                        value = value.model_dump(mode="json")
                    elif isinstance(value, dict):
                        # Validate dict by parsing it
                        value = ProfileShape.model_validate(value).model_dump(
                            mode="json"
                        )
                self._updates[name] = value
            else:
                super().__setattr__(name, value)


class EntPydanticTestMutatorDeletionAction(
    EntMutatorDeletionAction[
        ExampleViewerContext, EntPydanticTest, EntPydanticTestModel
    ]
):
    ent_type = EntPydanticTest


class EntPydanticTestExample:
    @classmethod
    async def gen_create(
        cls,
        vc: ExampleViewerContext,
        created_at: datetime | None = None,
        address: dict[str, Any] | Sentinel = NOTHING,
        name: str | Sentinel = NOTHING,
        legacy_data: dict[str, Any] | Sentinel = NOTHING,
        profile: dict[str, Any] | Sentinel = NOTHING,
    ) -> EntPydanticTest:
        # TODO make sure we only use this in test mode

        address = (
            {"street": "123 Main St", "city": "New York", "zip_code": "10001"}
            if isinstance(address, Sentinel)
            else address
        )

        name = "John Doe" if isinstance(name, Sentinel) else name

        legacy_data = (
            {"foo": "bar"} if isinstance(legacy_data, Sentinel) else legacy_data
        )

        profile = (
            {"bio": "Software engineer", "age": 30, "hobbies": ["coding", "reading"]}
            if isinstance(profile, Sentinel)
            else profile
        )

        return await EntPydanticTestMutator.create(
            vc=vc,
            created_at=created_at,
            address=address,
            name=name,
            legacy_data=legacy_data,
            profile=profile,
        ).gen_savex()


def _get_field(field_name: str) -> Field:
    schema = EntPydanticTestSchema()
    fields = schema.get_all_fields()
    field = next(
        filter(
            lambda field: field.name == field_name,
            fields,
        )
    )
    if not field:
        raise ValueError(f"Unknown field: {field_name}")
    return field
