####################
# Code generated by EntPy - do not modify manually!
####################

from __future__ import annotations
import logging
from entpy import (
    db,
    Ent,
    generate_uuid,
    Action,
    Decision,
)
from uuid import UUID
from datetime import datetime, UTC
from evc import ExampleViewerContext
from .ent_model import EntModel
from ent_parent_schema import EntParentSchema
from entpy import Field
from entpy import PrivacyError
from entpy.framework.ent import EntObjectBase
from entpy.framework.query import EntObjectQuery
from entpy.model import APIEntity
from functools import cache
from privacy import PrivacyMixin
from pydantic import Field as APIField
from sentinels import NOTHING, Sentinel  # type: ignore[import-untyped]
from sqlalchemy import ForeignKey
from sqlalchemy import String
from sqlalchemy import UUID as DBUUID
from sqlalchemy.orm import Mapped, mapped_column
from sqlalchemy.orm import relationship
from typing import TYPE_CHECKING

if TYPE_CHECKING:
    from .ent_grand_parent import EntGrandParentModel
    from .ent_grand_parent import EntGrandParentAPIModel
    from .ent_grand_parent import EntGrandParent

privacy_logger = logging.getLogger("entpy.privacy")


class EntParentModel(EntModel):
    __tablename__ = "parent"

    grand_parent_id: Mapped[UUID] = mapped_column(
        DBUUID(),
        ForeignKey("grand_parent.id", deferrable=True, initially="DEFERRED"),
        nullable=False,
    )
    name: Mapped[str] = mapped_column(String(100), nullable=False)
    grand_parent: Mapped["EntGrandParentModel"] = relationship(
        "EntGrandParentModel",
        primaryjoin="EntParentModel.grand_parent_id == EntGrandParentModel.id",
    )


class EntParentAPIModel(APIEntity):
    grand_parent: "EntGrandParentAPIModel" = APIField(...)
    name: str = APIField(..., examples=["Vincent"])


class EntParent(PrivacyMixin, EntObjectBase[ExampleViewerContext, EntParentModel]):
    m = EntParentModel
    schema = EntParentSchema()

    if TYPE_CHECKING:
        grand_parent_id: UUID
        name: str

        async def gen_grand_parent(self) -> "EntGrandParent":
            pass

    @classmethod
    @cache
    def _get_edge_type(cls, edge_name: str) -> tuple[type[Ent], bool]:
        match edge_name:
            case "grand_parent":
                from .ent_grand_parent import EntGrandParent

                return (EntGrandParent, False)

        return super()._get_edge_type(edge_name)

    @classmethod
    def query(cls, vc: ExampleViewerContext) -> EntParentQuery:
        return EntParentQuery(vc=vc)


class EntParentQuery(EntObjectQuery[ExampleViewerContext, EntParent, EntParentModel]):
    ent_type = EntParent
    model_type = EntParentModel


class EntParentMutator:
    @classmethod
    def create(
        cls,
        vc: ExampleViewerContext,
        grand_parent_id: UUID,
        name: str,
        id: UUID | None = None,
        created_at: datetime | None = None,
        updated_at: datetime | None = None,
    ) -> EntParentMutatorCreationAction:
        return EntParentMutatorCreationAction(
            vc=vc,
            id=id,
            created_at=created_at,
            updated_at=updated_at,
            grand_parent_id=grand_parent_id,
            name=name,
        )

    @classmethod
    def update(
        cls, vc: ExampleViewerContext, ent: EntParent
    ) -> EntParentMutatorUpdateAction:
        return EntParentMutatorUpdateAction(vc=vc, ent=ent)

    @classmethod
    def hard_delete(
        cls, vc: ExampleViewerContext, ent: EntParent
    ) -> EntParentMutatorDeletionAction:
        return EntParentMutatorDeletionAction(vc=vc, ent=ent, is_soft_delete=False)

    @classmethod
    def soft_delete(
        cls, vc: ExampleViewerContext, ent: EntParent
    ) -> EntParentMutatorDeletionAction:
        return EntParentMutatorDeletionAction(vc=vc, ent=ent, is_soft_delete=True)


class EntParentMutatorCreationAction:
    vc: ExampleViewerContext
    id: UUID
    grand_parent_id: UUID
    name: str

    def __init__(
        self,
        vc: ExampleViewerContext,
        id: UUID | None,
        created_at: datetime | None,
        updated_at: datetime | None,
        grand_parent_id: UUID,
        name: str,
    ) -> None:
        self.vc = vc
        self.created_at = created_at if created_at else datetime.now(tz=UTC)
        self.updated_at = updated_at if updated_at else self.created_at
        self.id = id if id else generate_uuid(EntParent, self.created_at)
        self.grand_parent_id = grand_parent_id
        self.name = name

    async def gen_savex(self) -> EntParent:
        model = EntParentModel(
            id=self.id,
            updated_at=self.updated_at,
            created_at=self.created_at,
            grand_parent_id=self.grand_parent_id,
            name=self.name,
        )
        db.session.add(model)
        ent = EntParent(vc=self.vc, model=model)
        decision = await ent.gen_evaluate_privacy(vc=self.vc, action=Action.CREATE)
        if decision != Decision.ALLOW:
            raise PrivacyError(
                f"Current viewer context is not authorized to CREATE EntParent with ID {ent.id}"
            )
        await db.session.flush()
        return await EntParent._genx_from_model(self.vc, model)  # noqa: SLF001


class EntParentMutatorUpdateAction:
    vc: ExampleViewerContext
    ent: EntParent
    id: UUID
    grand_parent_id: UUID
    name: str

    def __init__(self, vc: ExampleViewerContext, ent: EntParent) -> None:
        self.vc = vc
        self.ent = ent
        self.grand_parent_id = ent.grand_parent_id
        self.name = ent.name

    async def gen_savex(self) -> EntParent:
        model = self.ent.model
        model.grand_parent_id = self.grand_parent_id
        model.name = self.name
        model.updated_at = datetime.now(tz=UTC)
        db.session.add(model)
        new_ent = EntParent(vc=self.vc, model=model)
        decision = await new_ent.gen_evaluate_privacy(vc=self.vc, action=Action.UPDATE)
        if decision != Decision.ALLOW:
            raise PrivacyError(
                f"Current viewer context is not authorized to UPDATE EntParent with ID {new_ent.id}"
            )
        await db.session.flush()
        await db.session.refresh(model)
        return await EntParent._genx_from_model(self.vc, model)  # noqa: SLF001


class EntParentMutatorDeletionAction:
    vc: ExampleViewerContext
    ent: EntParent

    def __init__(
        self, vc: ExampleViewerContext, ent: EntParent, is_soft_delete: bool
    ) -> None:
        self.vc = vc
        self.ent = ent
        self.is_soft_delete = is_soft_delete

    async def gen_save(self) -> None:
        model = self.ent.model
        action = Action.SOFT_DELETE if self.is_soft_delete else Action.HARD_DELETE
        decision = await self.ent.gen_evaluate_privacy(vc=self.vc, action=action)
        if decision != Decision.ALLOW:
            raise PrivacyError(
                f"Current viewer context is not authorized to {action} EntParent with ID {self.ent.id}"
            )
        if self.is_soft_delete:
            model.soft_deleted_at = datetime.now(tz=UTC)
            model.updated_at = datetime.now(tz=UTC)
            db.session.add(model)
        else:
            await db.session.delete(model)
        await db.session.flush()


class EntParentExample:
    @classmethod
    async def gen_create(
        cls,
        vc: ExampleViewerContext,
        created_at: datetime | None = None,
        grand_parent_id: UUID | Sentinel = NOTHING,
        name: str | Sentinel = NOTHING,
    ) -> EntParent:
        # TODO make sure we only use this in test mode

        if isinstance(grand_parent_id, Sentinel) or grand_parent_id is None:
            from .ent_grand_parent import EntGrandParentExample

            grand_parent_id_ent = await EntGrandParentExample.gen_create(vc)
            grand_parent_id = grand_parent_id_ent.id
        name = "Vincent" if isinstance(name, Sentinel) else name

        return await EntParentMutator.create(
            vc=vc, created_at=created_at, grand_parent_id=grand_parent_id, name=name
        ).gen_savex()


def _get_field(field_name: str) -> Field:
    schema = EntParentSchema()
    fields = schema.get_all_fields()
    field = next(
        filter(
            lambda field: field.name == field_name,
            fields,
        )
    )
    if not field:
        raise ValueError(f"Unknown field: {field_name}")
    return field
