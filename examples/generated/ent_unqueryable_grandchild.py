####################
# Code generated by EntPy - do not modify manually!
####################

from __future__ import annotations
import logging
from entpy import (
    Ent,
)
from uuid import UUID
from datetime import datetime
from evc import ExampleViewerContext
from .ent_unqueryable_middle import EntUnqueryableMiddleAPIModel
from .ent_unqueryable_middle import EntUnqueryableMiddleModel
from .ent_unqueryable_middle import IEntUnqueryableMiddle
from .ent_unqueryable_middle import IEntUnqueryableMiddleMutatorDeletionAction
from .ent_unqueryable_middle import IEntUnqueryableMiddleMutatorUpdateAction
from .ent_unqueryable_middle import IEntUnqueryableMiddlePending
from ent_unqueryable_grandchild_schema import EntUnqueryableGrandchildSchema
from entpy.framework.ent import EntObjectBase
from entpy.framework.mutators import (
    EntMutatorCreationAction,
    EntMutatorUpdateAction,
    EntMutatorDeletionAction,
)
from entpy.framework.query import EntObjectQuery
from functools import cache
from privacy import PrivacyMixin
from pydantic import Field as APIField
from sentinels import NOTHING, Sentinel  # type: ignore[import-untyped]
from sqlalchemy import String
from sqlalchemy.orm import Mapped, mapped_column
from typing import TYPE_CHECKING

if TYPE_CHECKING:
    from entpy import Ent
    from entpy import Field

privacy_logger = logging.getLogger("entpy.privacy")


class EntUnqueryableGrandchildModel(EntUnqueryableMiddleModel):
    __tablename__ = "unqueryable_grandchild"

    detail: Mapped[str | None] = mapped_column(String(100), nullable=True)


class EntUnqueryableGrandchildAPIModel(EntUnqueryableMiddleAPIModel):
    detail: str | None = APIField(None, examples=["a detail"])


class EntUnqueryableGrandchildPending(IEntUnqueryableMiddlePending):
    m = EntUnqueryableGrandchildModel

    if TYPE_CHECKING:

        @property
        def detail(self) -> str | None:
            pass


class EntUnqueryableGrandchild(
    PrivacyMixin,
    EntObjectBase[ExampleViewerContext, EntUnqueryableGrandchildModel],
    IEntUnqueryableMiddle,
    EntUnqueryableGrandchildPending,
):
    m = EntUnqueryableGrandchildModel
    schema = EntUnqueryableGrandchildSchema()

    @classmethod
    @cache
    def _get_edge_type(cls, edge_name: str) -> tuple[type[Ent], bool]:

        return super()._get_edge_type(edge_name)

    @classmethod
    def query(  # type: ignore[override]
        cls,
        vc: ExampleViewerContext,
    ) -> EntUnqueryableGrandchildQuery:
        return EntUnqueryableGrandchildQuery(vc=vc)


class EntUnqueryableGrandchildQuery(
    EntObjectQuery[
        ExampleViewerContext, EntUnqueryableGrandchild, EntUnqueryableGrandchildModel
    ]
):
    ent_type = EntUnqueryableGrandchild
    model_type = EntUnqueryableGrandchildModel


class EntUnqueryableGrandchildMutator:
    @classmethod
    def create(
        cls,
        vc: ExampleViewerContext,
        kind: str,
        label: str,
        detail: str | None = None,
        id: UUID | None = None,
        created_at: datetime | None = None,
        updated_at: datetime | None = None,
    ) -> EntUnqueryableGrandchildMutatorCreationAction:
        return EntUnqueryableGrandchildMutatorCreationAction(
            vc=vc,
            id=id,
            created_at=created_at,
            updated_at=updated_at,
            kind=kind,
            label=label,
            detail=detail,
        )

    @classmethod
    def update(
        cls, vc: ExampleViewerContext, ent: EntUnqueryableGrandchild
    ) -> EntUnqueryableGrandchildMutatorUpdateAction:
        return EntUnqueryableGrandchildMutatorUpdateAction(vc=vc, ent=ent)

    @classmethod
    def hard_delete(
        cls, vc: ExampleViewerContext, ent: EntUnqueryableGrandchild
    ) -> EntUnqueryableGrandchildMutatorDeletionAction:
        return EntUnqueryableGrandchildMutatorDeletionAction(
            vc=vc, ent=ent, is_soft_delete=False
        )

    @classmethod
    def soft_delete(
        cls, vc: ExampleViewerContext, ent: EntUnqueryableGrandchild
    ) -> EntUnqueryableGrandchildMutatorDeletionAction:
        return EntUnqueryableGrandchildMutatorDeletionAction(
            vc=vc, ent=ent, is_soft_delete=True
        )


class EntUnqueryableGrandchildMutatorCreationAction(
    EntMutatorCreationAction[
        ExampleViewerContext, EntUnqueryableGrandchild, EntUnqueryableGrandchildModel
    ]
):
    ent_type = EntUnqueryableGrandchild
    model_type = EntUnqueryableGrandchildModel
    pending_type = EntUnqueryableGrandchildPending
    schema = EntUnqueryableGrandchildSchema()
    vc: ExampleViewerContext

    if TYPE_CHECKING:
        id: UUID
        kind: str
        label: str
        detail: str | None = None


class EntUnqueryableGrandchildMutatorUpdateAction(
    EntMutatorUpdateAction[
        ExampleViewerContext, EntUnqueryableGrandchild, EntUnqueryableGrandchildModel
    ],
    IEntUnqueryableMiddleMutatorUpdateAction,
):
    ent_type = EntUnqueryableGrandchild
    model_type = EntUnqueryableGrandchildModel
    pending_type = EntUnqueryableGrandchildPending
    schema = EntUnqueryableGrandchildSchema()
    vc: ExampleViewerContext
    ent: EntUnqueryableGrandchild

    if TYPE_CHECKING:
        id: UUID
        kind: str
        label: str
        detail: str | None = None


class EntUnqueryableGrandchildMutatorDeletionAction(  # type: ignore[misc]
    EntMutatorDeletionAction[
        ExampleViewerContext, EntUnqueryableGrandchild, EntUnqueryableGrandchildModel
    ],
    IEntUnqueryableMiddleMutatorDeletionAction,
):
    ent_type = EntUnqueryableGrandchild
    pending_type = EntUnqueryableGrandchildPending


class EntUnqueryableGrandchildExample:
    @classmethod
    async def gen_create(
        cls,
        vc: ExampleViewerContext,
        created_at: datetime | None = None,
        kind: str | Sentinel = NOTHING,
        label: str | Sentinel = NOTHING,
        detail: str | Sentinel = NOTHING,
    ) -> EntUnqueryableGrandchild:
        # TODO make sure we only use this in test mode

        kind = "a kind" if isinstance(kind, Sentinel) else kind

        label = "a label" if isinstance(label, Sentinel) else label

        detail = "a detail" if isinstance(detail, Sentinel) else detail

        return await EntUnqueryableGrandchildMutator.create(
            vc=vc, created_at=created_at, kind=kind, label=label, detail=detail
        ).gen_savex()


def _get_field(field_name: str) -> Field:
    schema = EntUnqueryableGrandchildSchema()
    fields = schema.get_all_fields()
    field = next(
        filter(
            lambda field: field.name == field_name,
            fields,
        )
    )
    if not field:
        raise ValueError(f"Unknown field: {field_name}")
    return field
