####################
# Code generated by EntPy - do not modify manually!
####################

from __future__ import annotations
import logging
from entpy import (
    Ent,
)
from uuid import UUID
from datetime import datetime
from evc import ExampleViewerContext
from .ent_model import EntModel
from ent_credential_schema import EntCredentialSchema
from entpy.framework.ent import EntObjectBase
from entpy.framework.ent import EntPending
from entpy.framework.mutators import (
    EntMutatorCreationAction,
    EntMutatorUpdateAction,
    EntMutatorDeletionAction,
)
from entpy.framework.query import EntObjectQuery
from entpy.model import APIEntity
from functools import cache
from privacy import PrivacyMixin
from pydantic import Field as APIField
from sentinels import NOTHING, Sentinel  # type: ignore[import-untyped]
from sqlalchemy import String
from sqlalchemy.orm import Mapped, mapped_column
from typing import TYPE_CHECKING

if TYPE_CHECKING:
    from entpy import Ent
    from entpy import Field

privacy_logger = logging.getLogger("entpy.privacy")


class EntCredentialModel(EntModel):
    __tablename__ = "credential"

    name: Mapped[str] = mapped_column(String(100), nullable=False)
    slug: Mapped[str | None] = mapped_column(String(100), nullable=True)


class EntCredentialAPIModel(APIEntity):
    name: str = APIField(..., examples=["My Credential"])
    slug: str | None = APIField(None, examples=["my-credential"])


class EntCredentialPending(EntPending[EntCredentialModel]):
    m = EntCredentialModel

    if TYPE_CHECKING:

        @property
        def name(self) -> str:
            pass

        @property
        def slug(self) -> str | None:
            pass


class EntCredential(
    PrivacyMixin,
    EntObjectBase[ExampleViewerContext, EntCredentialModel],
    EntCredentialPending,
):
    m = EntCredentialModel
    schema = EntCredentialSchema()

    @classmethod
    @cache
    def _get_edge_type(cls, edge_name: str) -> tuple[type[Ent], bool]:

        return super()._get_edge_type(edge_name)

    @classmethod
    def query(
        cls,
        vc: ExampleViewerContext,
    ) -> EntCredentialQuery:
        return EntCredentialQuery(vc=vc)


class EntCredentialQuery(
    EntObjectQuery[ExampleViewerContext, EntCredential, EntCredentialModel]
):
    ent_type = EntCredential
    model_type = EntCredentialModel


class EntCredentialMutator:
    @classmethod
    def create(
        cls,
        vc: ExampleViewerContext,
        name: str,
        slug: str | None = None,
        id: UUID | None = None,
        created_at: datetime | None = None,
        updated_at: datetime | None = None,
    ) -> EntCredentialMutatorCreationAction:
        return EntCredentialMutatorCreationAction(
            vc=vc,
            id=id,
            created_at=created_at,
            updated_at=updated_at,
            name=name,
            slug=slug,
        )

    @classmethod
    def update(
        cls, vc: ExampleViewerContext, ent: EntCredential
    ) -> EntCredentialMutatorUpdateAction:
        return EntCredentialMutatorUpdateAction(vc=vc, ent=ent)

    @classmethod
    def hard_delete(
        cls, vc: ExampleViewerContext, ent: EntCredential
    ) -> EntCredentialMutatorDeletionAction:
        return EntCredentialMutatorDeletionAction(vc=vc, ent=ent, is_soft_delete=False)

    @classmethod
    def soft_delete(
        cls, vc: ExampleViewerContext, ent: EntCredential
    ) -> EntCredentialMutatorDeletionAction:
        return EntCredentialMutatorDeletionAction(vc=vc, ent=ent, is_soft_delete=True)


class EntCredentialMutatorCreationAction(
    EntMutatorCreationAction[ExampleViewerContext, EntCredential, EntCredentialModel]
):
    ent_type = EntCredential
    model_type = EntCredentialModel
    pending_type = EntCredentialPending
    schema = EntCredentialSchema()
    vc: ExampleViewerContext

    if TYPE_CHECKING:
        id: UUID
        name: str
        slug: str | None = None


class EntCredentialMutatorUpdateAction(
    EntMutatorUpdateAction[ExampleViewerContext, EntCredential, EntCredentialModel]
):
    ent_type = EntCredential
    model_type = EntCredentialModel
    pending_type = EntCredentialPending
    schema = EntCredentialSchema()
    vc: ExampleViewerContext
    ent: EntCredential

    if TYPE_CHECKING:
        id: UUID
        name: str
        slug: str | None = None


class EntCredentialMutatorDeletionAction(
    EntMutatorDeletionAction[ExampleViewerContext, EntCredential, EntCredentialModel]
):
    ent_type = EntCredential
    pending_type = EntCredentialPending


class EntCredentialExample:
    @classmethod
    async def gen_create(
        cls,
        vc: ExampleViewerContext,
        created_at: datetime | None = None,
        name: str | Sentinel = NOTHING,
        slug: str | Sentinel = NOTHING,
    ) -> EntCredential:
        # TODO make sure we only use this in test mode

        name = "My Credential" if isinstance(name, Sentinel) else name

        slug = "my-credential" if isinstance(slug, Sentinel) else slug

        return await EntCredentialMutator.create(
            vc=vc, created_at=created_at, name=name, slug=slug
        ).gen_savex()


def _get_field(field_name: str) -> Field:
    schema = EntCredentialSchema()
    fields = schema.get_all_fields()
    field = next(
        filter(
            lambda field: field.name == field_name,
            fields,
        )
    )
    if not field:
        raise ValueError(f"Unknown field: {field_name}")
    return field
